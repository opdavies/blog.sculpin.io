{
    "require": {
        "sculpin/sculpin": "3.3.0-alpha5"
    },
    "scripts": {
        "watch": [
            "Composer\\Config::disableProcessTimeout",
            "vendor/bin/sculpin generate --watch --server"
        ],
        "publish": [
            "Composer\\Config::disableProcessTimeout",
            "vendor/bin/sculpin generate --env=prod --output-dir=docs"
        ],
        "new": [
            "Composer\\Config::disableProcessTimeout",
            "./new.sh"
        ]
    },
    "config": {
        "allow-plugins": {
            "sculpin/sculpin-theme-composer-plugin": true
        }
    }
}
